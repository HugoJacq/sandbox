# 

- verify that evolution of mixed layer depth is sqrt(time)
- MLD can be diag with min(<w'T'>)


- add dimensions to the problem, and check with Bas. check.
- no outputs when using gpus -> netcdf_bas.h ? (ok when using 1 cpu or in mpi
with >1 cpu)
